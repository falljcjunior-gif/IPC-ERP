# ══════════════════════════════════════════════════════════════════════════════
# IPC ERP — CONFIGURATION DES VARIABLES D'ENVIRONNEMENT
# ══════════════════════════════════════════════════════════════════════════════

# ── FIREBASE CLIENT CONFIG ───────────────────────────────────────────────────
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# ── PUSH NOTIFICATIONS (FCM) ─────────────────────────────────────────────────
# Générée dans Console Firebase -> Paramètres du projet -> Cloud Messaging -> Web Configuration
VITE_FCM_VAPID_KEY=your_vapid_public_key

# ── WEBRTC (STUN/TURN) ───────────────────────────────────────────────────────
# Requis pour les appels vidéo derrière des pare-feux d'entreprise / NAT strict.
# Service recommandé : Metered.ca ou Twilio.
VITE_TURN_URL=turn:your-turn-server.com:3478
VITE_TURN_USERNAME=your_username
VITE_TURN_CREDENTIAL=your_password

# ── AXELOR SSOT (BACKEND ONLY) ───────────────────────────────────────────────
AXELOR_API_URL=https://votre-instance-axelor.com
AXELOR_USERNAME=admin
AXELOR_PASSWORD=admin

# ── NEXUS AI (BACKEND ONLY) ──────────────────────────────────────────────────
OPENAI_API_KEY=sk-your-openai-key

# ── FIREBASE APP CHECK (reCAPTCHA ENTERPRISE) ────────────────────────────────
# NB : Il s'agit de reCAPTCHA **Enterprise** (pas v3 ni v2).
# SETUP (une seule fois) :
#   1. Google Cloud Console → Security → reCAPTCHA Enterprise → Create key
#      → Type : Web → Ajouter votre domaine → Copier la clé de site
#   2. Firebase Console → App Check → Enregistrer l'app web
#      → Choisir reCAPTCHA Enterprise → Coller la clé de site
#   3. En DEV : copier le debug token affiché dans la console
#      → Firebase Console → App Check → Add debug token
#   4. Décommenter enforceAppCheck: true dans chaque Cloud Function
# La clé de site est publique (côté client) — ne pas mettre la clé secrète ici.
VITE_ENABLE_APPCHECK=false
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_enterprise_site_key
# Token debug optionnel pour le développement local uniquement.
VITE_APPCHECK_DEBUG_TOKEN=your_local_debug_token
